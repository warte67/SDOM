#!/usr/bin/env bash
# =============================================================================
# SDOM Version Increment Script
# -----------------------------------------------------------------------------
# Description:
#   This script manually triggers SDOM’s version number regeneration by
#   invoking the central `gen_version.sh` utility. It updates the patch version
#   and refreshes metadata such as the build date, platform, compiler, and branch.
#
# Usage:
#   ./update_version.sh
#
# Behavior:
#   - Executes `../../scripts/gen_version.sh`
#   - Should be run from the `tests/` directory
#   - Outputs updated version information into the SDOM build headers
#
# Notes:
#   - This is a manual alternative to CMake’s automatic version generation.
#   - Use this when you want to increment the patch number between commits
#     without triggering a full rebuild.
#
# Author: Jay Faries (warte67)
# =============================================================================

../../scripts/gen_version.sh
